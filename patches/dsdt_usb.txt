# disable XWAK
into method label XWAK code_regex ([\s\S]*)(Return[\s\S]*) replace_content
begin
                Return(0)\n
                If (LOr (LEqual (XUSB, One), LEqual (XRST, One)))\n
                {\n
                    XSEL ()\n
                }\n
end;
